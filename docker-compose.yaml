version: '3'
services:

  web:
    container_name: web
    image: mingulmangul/mg-web
    expose:
      - 8080
    ports:
      - 8080:8080

  nginx:
    container_name: nginx
    image: mingulmangul/mg-nginx
    ports:
      - 80:80
    depends_on:
      - web
